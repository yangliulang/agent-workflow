---
description: 前端 Web 开发规范（@FRONTEND_PATH@）
globs: @FRONTEND_GLOB@
alwaysApply: false
---

# Web 开发规范

> 由 `./scripts/sync-project-rules.sh` 根据 `pipeline.project.yaml` 生成，勿手改路径。

完整说明见 `handoff/conventions/frontend.md`。编写 `@FRONTEND_PATH@/` 时遵守。

## 栈

- Vite + React 函数组件
- 页面：`src/pages/`；API：`src/api/`

## 对接

- baseURL：`@API_BASE_URL@`
- `code === 0` 用 `data`；否则展示 `message`
- 结构须与 OpenAPI 一致

## 页面

- 路由与交互以 `brief.md` + `frontend/integration.md` 为准
- 须覆盖主流程 + brief 要求的错误态
- 提交中禁用按钮或显示 loading

## Token

仅按 `brief.md` 约定存储；无约定则不擅自加 localStorage。

## 功能开发时

- 联调后更新 `frontend/integration.md`
- 流转门禁见 `frontend-agent.mdc`
