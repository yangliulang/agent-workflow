---
description: 后端 API 开发规范（@BACKEND_PATH@）
globs: @BACKEND_GLOB@
alwaysApply: false
---

# API 开发规范

> 由 `./scripts/sync-project-rules.sh` 根据 `pipeline.project.yaml` 生成，勿手改路径。

完整说明见 `handoff/conventions/backend.md`。编写 `@BACKEND_PATH@/` 时遵守。

## 实现

- Express + ESM；路由与 `api.openapi.yaml` 一致
- 成功：`{ code: 0, message, data? }`
- 4xx：`{ code, message }`，code 与 OpenAPI 示例一致

## 跨域

允许 `@WEB_DEV_URL@`。

## 配置

`PORT` 默认 3000；不写死密钥。

## 功能开发时

- 只改当前功能包所需代码
- 完成后更新该功能包的 `backend/notes.md`（含 curl）
- 流转门禁见 `backend-agent.mdc`
