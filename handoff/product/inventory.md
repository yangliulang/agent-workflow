# 存量切口清单（inventory）

> **用途**：项目已开发一段时间后接入流水线时，划清「PRD 全量」与「本阶段 backlog」的边界。  
> 指挥官在 `/pipeline-product-plan` 时 `@` 本文件（可同时 `@` PRD）。  
> **维护人**：产品 / 指挥官（人工填写；Agent 只读，不自动改本文件）。

最后更新：`YYYY-MM-DD`  
对应 roadmap：`handoff/roadmap/phase-N.md`（填写 N）

---

## 1. 切口说明（一句话）

<!-- 例：v1.0 已上线登录与首页；Phase 1 只做资料编辑与导出，不碰支付。 -->

---

## 2. 已实现（代码已有，本阶段默认不建包、不走 7 步）

> 仅作对照与依赖说明；**不要**为每一项都补功能包，除非下方「需补契约」标明。

| 模块 / 能力 | 代码位置或路由（可选） | 发布版本 / 日期（可选） | 备注 |
|-------------|------------------------|-------------------------|------|
| 例：用户登录 | `apps/api/src/auth` | v1.0 | JWT 已通 |
| | | | |

### 需补契约的已实现项（可选）

> 历史功能缺 OpenAPI / 测试追溯，本阶段只补文档、小改时可列入 roadmap 为 `planned`。

| 能力 | 建议功能 ID | 原因 |
|------|-------------|------|
| | `YYYY-MM-DD--slug` | |

---

## 3. 进行中 / 半成品（优先进 backlog）

| 能力 | 当前状态 | 阻塞 / 风险 | 建议功能 ID |
|------|----------|-------------|-------------|
| 例：用户资料页 UI 已有，API 未对齐 | 前端 80% | 无 OpenAPI | `YYYY-MM-DD--user-profile` |
| | | | |

---

## 4. 本阶段要做（→ 写入 roadmap backlog）

> `product.plan` 主要依据本节生成 `phase-N.md` 表格；一行一功能，不写 API 细节。

| 优先级 | 能力名 | 一句话用户价值 | 依赖 | 建议功能 ID |
|--------|--------|----------------|------|-------------|
| P0 | | | 无 / 见上表 | `YYYY-MM-DD--slug` |
| P1 | | | | |
| P2 | | | | |

**本阶段第一个 `product.contract` 候选**（P0、无依赖、可单包闭环）：

- 功能名：
- 建议功能 ID：

---

## 5. 本阶段明确不做

-

---

## 6. 与 PRD 的对应关系（从哪里读 PRD）

| PRD 章节 / 位置 | 如何处理 |
|-----------------|----------|
| 例：§3 已发布功能 | 对照 §2 已实现，不进入 backlog |
| 例：§5 Phase 2 规划 | 对照 §5 本阶段不做 或 下一 phase |
| 例：§6 非功能 | 抄到 roadmap「非功能（阶段共识）」 |

PRD 路径（见 `pipeline.project.yaml` → `project.prd.primary`）：

- `<!-- 填写，如 PRD.md 或 handoff/product/prd.md -->`

---

## 7. 非功能（本阶段共识，可选）

与 [project.md](../conventions/project.md) 冲突时以功能包契约为准。

| 项 | 值 |
|----|-----|
| API 基址 | 见 `pipeline.project.yaml` |
| Web 基址 | 见 `pipeline.project.yaml` |
| 统一响应 | `{ "code", "message", "data" }` |

---

## 8. 变更记录

| 日期 | 变更 | 操作人 |
|------|------|--------|
| | 创建 inventory | |
